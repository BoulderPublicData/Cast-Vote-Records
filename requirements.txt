# pip equivalent of pyproject.toml's [project] + [analysis] extras.
# The authoritative source is pyproject.toml; this file is kept in sync
# for the convenience of consumers who prefer `pip install -r requirements.txt`.
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
requests>=2.31
pandera[pandas]>=0.20
tabulate>=0.9
# analysis extras (clusters.ipynb)
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
umap-learn>=0.5.5
hdbscan>=0.8.33
